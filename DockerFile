# 🐳 Base image with JDK 17 (you can change to 21 or 11 if needed)
FROM eclipse-temurin:17-jdk-alpine

# 👨‍💻 Set the working directory
WORKDIR /app

# 🔁 Copy the built jar file into the container
COPY target/*.jar app.jar

# 🚪 Expose port (match your server.port in application.properties or default 8080)
EXPOSE 8080

# 🚀 Run the ja
ENTRYPOINT ["java", "-jar", "app.jar"]
